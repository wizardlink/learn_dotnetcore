@model CityWeather

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title></title>
        <link href="~/StyleSheet.css" rel="stylesheet">
    </head>
    <body>
        <div class="container">
            <div class="grid">
                <div class="details">
                    <span class="name">@Model.CityName</span>
                    <span class="time">@Model.DateAndTime.ToString("H:mm tt")</span>
                </div>
                <hr />
                <div>
                    <span>@Model.TemperatureFahrenheit</span>
                    <span class="degree">°F</span>
                </div>
            </div>
            <a href="/" class="backButton">Back to Weather Home Page</a>
        </div>
    </body>
</html>
